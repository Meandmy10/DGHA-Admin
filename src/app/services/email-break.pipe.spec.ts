import { EmailBreakPipe } from './email-break.pipe';

describe('EmailBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
